import javax.swing.*;
import java.awt.*;

public class MyFrame extends JFrame {

    public void paint(Graphics g) {
        super.paint(g);
    }

    MyFrame(String title) {
        super(title);
    }
}